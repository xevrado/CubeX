"// ---- Admin Det Update Score Button (AnlÄ±k ve En YÃ¼ksek ayrÄ± ayrÄ±) ----\nconst adminDetUpdateScoreBtn = document.getElementById('adminDetUpdateScoreBtn');\nif (adminDetUpdateScoreBtn) {\n  adminDetUpdateScoreBtn.addEventListener('click', () => {\n    sfxClick();\n    const activeVal = parseInt(document.getElementById('adminDetActiveScore').value);\n    const bestVal = parseInt(document.getElementById('adminDetBestScore').value);\n    const nameEl = document.getElementById('detName');\n    const name = nameEl ? nameEl.textContent.trim() : '';\n    if (!name || name === '--') {\n      showCustomAlert('Hata', 'GÃ¼ncellemek iÃ§in soldan bir kullanÄ±cÄ± seÃ§melisiniz.', 'error');\n      return;\n    }\n    if (isNaN(activeVal) && isNaN(bestVal)) {\n      showCustomAlert('Hata', 'GÃ¼ncellemek iÃ§in en az bir skor deÄŸeri girin.', 'error');\n      return;\n    }\n    adminDetUpdateScoreBtn.innerHTML = '<i class=\"fas fa-spinner fa-spin\"></i> GÃ¼ncelleniyor...';\n    adminUpdateScores(name, activeVal, bestVal)\n    .then(res => {\n      if (res.ok) {\n        showCustomAlert('BaÅŸarÄ±lÄ±', `${name} kullanÄ±cÄ±sÄ±nÄ±n skorlarÄ± gÃ¼ncellendi.`, 'success');\n        document.getElementById('adminDetActiveScore').value = '';\n        document.getElementById('adminDetBestScore').value = '';\n        loadAdminPanelData();\n        loadLeaderboard();\n      } else {\n        showCustomAlert('Hata', 'Skor gÃ¼ncellenirken bir hata oluÅŸtu.', 'error');\n      }\n    })\n    .catch(err => {\n      console.error(err);\n      showCustomAlert('Hata', 'AÄŸ hatasÄ±.', 'error');\n    })\n    .finally(() => {\n      adminDetUpdateScoreBtn.innerHTML = '<i class=\"fas fa-floppy-disk\"></i> SkorlarÄ± GÃ¼ncelle';\n    });\n  });\n}\n\n// Cihaz ID'sinden 6 haneli benzersiz, okunabilir bir Hesap ID'si Ã¼retir\nfunction getAccountId(deviceId) {\n  if (!deviceId || deviceId === 'Bilinmiyor') return '------';\n  let hash = 0;\n  for (let i = 0; i < deviceId.length; i++) {\n    hash = (hash << 5) - hash + deviceId.charCodeAt(i);\n    hash |
<truncated 21502 bytes>
